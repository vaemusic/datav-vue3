[build.environment]
  NPM_FLAGS = "--version"
  NODE_VERSION = "16"

[build]
  base = "/"
  publish = "npx pnpm i --store=node_modules/.pnpm-store && npx pnpm run build:docs"
  command = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
